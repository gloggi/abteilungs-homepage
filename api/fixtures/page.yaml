App\Entity\Page:
    page_{1..50}:
        title: <sentence(4)>
        route: <slug()>
        createdAt: <identity(new \DateTimeImmutable('now'))>
        updatedAt: <identity(new \DateTimeImmutable('now'))>